{
  "name": "scratch-from-start",
  "version": "0.2.0",
  "description": "",
  "scripts": {
    "watch:sass": "sass --no-source-map --watch src/styles:dist/css",
    "start:sass": "sass src/styles:dist/css",
    "build:sass": "sass src/styles:dist/css --style compressed",
    "watch:eleventy": "eleventy --serve",
    "build:eleventy": "eleventy",
    "start": "npm-run-all start:sass --parallel watch:*",
    "build": "npm-run-all build:*",
    "serve": "serve dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:codenomnom/scratch-froms-tart.git"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2",
    "@11ty/eleventy-plugin-syntaxhighlight": "^4.1.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.55.0"
  }
}
